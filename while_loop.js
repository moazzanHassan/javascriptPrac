var incrementor = 0;

var text = "";

while(incrementor < 10){
    text += `here is a incrementor ${incrementor} and the square of it is ${incrementor * incrementor} \n`;
    incrementor++
}
console.log(text)